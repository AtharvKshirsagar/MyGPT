from setuptools import setup

setup(name='myGPT',
      version='0.0.1',
      author='CS335',
      packages=['mygpt'],
      description='A PyTorch re-implementation of GPT',
      license='Atharv',
      install_requires=[
            'torch',
      ],
)
